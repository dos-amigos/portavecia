---
phase: 03-engagement-features
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - site/blueprints/pages/events.yml
  - site/controllers/events.php
  - site/templates/events.php
  - site/snippets/sections/event-card.php
  - content/7_events/events.it.txt
  - content/7_events/events.en.txt
  - site/languages/it.php
  - site/languages/en.php
autonomous: true
requirements:
  - EVNT-01
  - EVNT-02
  - EVNT-03
must_haves:
  truths:
    - "Events page displays upcoming events with date, description, image, and WhatsApp booking CTA"
    - "Past events appear in a separate section with dimmed/faded styling"
    - "Each event card shows title, date+time, description, photo, and optional prenota button"
    - "Bar owner can add, edit, and remove events from Kirby Panel"
    - "Events page works in both Italian and English"
  artifacts:
    - path: "site/blueprints/pages/events.yml"
      provides: "Events page blueprint with structure field"
      contains: "structure"
    - path: "site/controllers/events.php"
      provides: "Controller splitting events into upcoming and past"
      contains: "upcoming"
    - path: "site/templates/events.php"
      provides: "Events page template with upcoming/past sections"
      min_lines: 30
    - path: "site/snippets/sections/event-card.php"
      provides: "Event card component adapted from dish-card pattern"
      min_lines: 15
    - path: "content/7_events/events.it.txt"
      provides: "Events content with 4 placeholder events in Italian"
    - path: "content/7_events/events.en.txt"
      provides: "Events content with 4 placeholder events in English"
  key_links:
    - from: "site/controllers/events.php"
      to: "site/templates/events.php"
      via: "compact('upcoming', 'past') variables"
      pattern: "compact.*upcoming.*past"
    - from: "site/templates/events.php"
      to: "site/snippets/sections/event-card.php"
      via: "snippet('sections/event-card') with $event and $isPast"
      pattern: "snippet.*event-card"
    - from: "site/snippets/sections/event-card.php"
      to: "WhatsApp API"
      via: "wa.me link with rawurlencode"
      pattern: "wa\\.me"
---

<objective>
Build the Events page with upcoming/past event cards, WhatsApp booking CTAs, and 4 placeholder events -- fully editable from Kirby Panel with bilingual IT/EN content.

Purpose: Visitors discover upcoming degustazioni, special evenings, and themed events, and can book via WhatsApp with one tap.
Output: Working events page at /eventi (IT) and /en/events (EN) with 4 placeholder events (3 upcoming, 1 past).
</objective>

<execution_context>
@C:\Users\boxwe\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\boxwe\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-engagement-features/03-CONTEXT.md
@.planning/phases/03-engagement-features/03-RESEARCH.md
@.planning/phases/03-engagement-features/03-UI-SPEC.md

@site/templates/wines.php
@site/snippets/sections/dish-card.php
@site/controllers/site.php
@site/blueprints/pages/wines.yml
@site/languages/it.php
@site/languages/en.php

<interfaces>
<!-- Established patterns from Phase 1-2 that executor MUST follow -->

Template structure (from wines.php):
```php
<?php snippet('layout/header') ?>
<main>
  <section class="section-padding bg-dark">
    <div class="container-site text-center">
      <h1 class="font-heading text-4xl md:text-5xl text-primary mb-4"><?= $page->headline() ?></h1>
    </div>
  </section>
</main>
<?php snippet('layout/footer') ?>
```

Card pattern (from dish-card.php):
```php
<div class="bg-dark/50 border border-light/10 rounded-lg overflow-hidden flex flex-col md:flex-row">
  <div class="md:w-1/3 h-48 md:h-auto">
    <img src="thumb-url" class="w-full h-full object-cover" loading="lazy">
  </div>
  <div class="p-6 flex-1 flex flex-col justify-between">
    <!-- content -->
  </div>
</div>
```

Global data (from site.php controller): $phone, $whatsapp, $email, $address, $hours, $social

Structure field pattern (from wines.yml): type: structure, sortable: true, with typed fields
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Events blueprint, controller, and content with placeholder events</name>
  <files>
    site/blueprints/pages/events.yml,
    site/controllers/events.php,
    content/7_events/events.it.txt,
    content/7_events/events.en.txt
  </files>
  <read_first>
    site/blueprints/pages/wines.yml,
    site/controllers/site.php
  </read_first>
  <action>
1. Create `site/blueprints/pages/events.yml` per D-09 and D-12:
   - title: Eventi
   - Two tabs:
     - "content" tab: headline (text, required), intro (textarea, small)
     - "events" tab with structure field:
       - label: Lista Eventi
       - type: structure
       - sortable: true
       - fields:
         - title (text, width: 1/2, required)
         - event_date (date, width: 1/4, required)
         - event_time (time, width: 1/4)
         - description (textarea, size: small)
         - photo (files, query: page.images, multiple: false, width: 1/2)
         - whatsapp_booking (toggle, default: true, width: 1/2, text: ["No", "Si"])
   - Add images section for event photos (type: files, layout: cards)

2. Create `site/controllers/events.php` per D-07:
   ```php
   <?php
   return function ($page) {
       $now = time();
       $events = $page->events()->toStructure();

       $upcoming = $events->filter(function ($event) use ($now) {
           return $event->event_date()->toDate() >= $now;
       })->sortBy('event_date', 'asc');

       $past = $events->filter(function ($event) use ($now) {
           return $event->event_date()->toDate() < $now;
       })->sortBy('event_date', 'desc');

       return compact('upcoming', 'past');
   };
   ```

3. Create content directory `content/7_events/` with content files per D-08.

   `events.it.txt`:
   ```
   Title: Eventi
   ----
   Headline: Eventi
   ----
   Intro: Serate speciali, degustazioni e momenti da condividere
   ----
   Events:
   -
     title: Degustazione Amarone
     event_date: 2026-05-15
     event_time: "19:30"
     description: Una serata dedicata al re dei vini veneti. Degustazione guidata di tre annate di Amarone della Valpolicella, accompagnata da taglieri di formaggi stagionati e salumi selezionati. Posti limitati.
     whatsapp_booking: true
   -
     title: Serata Dim Sum
     event_date: 2026-05-22
     event_time: "20:00"
     description: Il meglio della tradizione cinese in formato aperitivo. Ravioli al vapore, involtini croccanti e wonton in brodo, il tutto accompagnato da una selezione di vini bianchi e bollicine.
     whatsapp_booking: true
   -
     title: Aperitivo d'Estate
     event_date: 2026-06-05
     event_time: "18:30"
     description: Inauguriamo la stagione estiva nel nostro plateatico con spritz, vini freschi e finger food della tradizione wenzhounese. Musica soft e atmosfera rilassata.
     whatsapp_booking: true
   -
     title: Wine & Wonton Night
     event_date: 2026-04-01
     event_time: "19:30"
     description: La nostra prima serata dedicata all'incontro tra vino italiano e cucina cinese. Quattro portate, quattro abbinamenti, un'esperienza unica.
     whatsapp_booking: false
   ```

   `events.en.txt`:
   ```
   Title: Events
   ----
   Headline: Events
   ----
   Intro: Special evenings, tastings, and moments to share
   ----
   Events:
   -
     title: Amarone Tasting
     event_date: 2026-05-15
     event_time: "19:30"
     description: An evening dedicated to the king of Veneto wines. Guided tasting of three vintages of Amarone della Valpolicella, paired with aged cheese and selected cured meats. Limited seats.
     whatsapp_booking: true
   -
     title: Dim Sum Night
     event_date: 2026-05-22
     event_time: "20:00"
     description: The best of Chinese tradition in aperitivo format. Steamed dumplings, crispy spring rolls, and wonton soup, all paired with a selection of white wines and sparkling.
     whatsapp_booking: true
   -
     title: Summer Aperitivo
     event_date: 2026-06-05
     event_time: "18:30"
     description: We kick off the summer season on our outdoor terrace with spritz, fresh wines, and Wenzhounese finger food. Soft music and a relaxed atmosphere.
     whatsapp_booking: true
   -
     title: Wine & Wonton Night
     event_date: 2026-04-01
     event_time: "19:30"
     description: Our first evening dedicated to the encounter between Italian wine and Chinese cuisine. Four courses, four pairings, one unique experience.
     whatsapp_booking: false
   ```

4. Copy 3-4 photos from _sources/ to content/7_events/ for use as event images:
   - menu-food.jpeg (for Dim Sum Night)
   - sala-interna.jpeg (for Aperitivo d'Estate)
   - Ravioli cinesi.jpeg (for Wine & Wonton Night)
   - piatto-pesce.jpeg (for Degustazione Amarone -- closest available)

NOTE: The "Wine & Wonton Night" event has date 2026-04-01 which is in the past (today is 2026-04-09), so it will appear in the "Eventi Passati" section. The other 3 events are future dates. whatsapp_booking is false for past event per D-09 (optional field).

IMPORTANT: Use `type: date` (not text) for event_date in blueprint. Kirby stores dates as YYYY-MM-DD. Use `type: time` for event_time separately. This avoids Pitfall 4 from RESEARCH.md.
  </action>
  <verify>
    <automated>php -r "echo file_exists('site/blueprints/pages/events.yml') && file_exists('site/controllers/events.php') && file_exists('content/7_events/events.it.txt') && file_exists('content/7_events/events.en.txt') ? 'OK' : 'FAIL';"</automated>
  </verify>
  <acceptance_criteria>
    - site/blueprints/pages/events.yml exists with structure field containing title, event_date (type: date), event_time (type: time), description, photo, whatsapp_booking
    - site/controllers/events.php exists with $upcoming and $past variables using toDate() filtering
    - content/7_events/events.it.txt contains 4 events with Italian descriptions
    - content/7_events/events.en.txt contains 4 events with English descriptions
    - 1 event has past date (2026-04-01), 3 events have future dates
    - Event photos copied from _sources/ to content/7_events/
  </acceptance_criteria>
  <done>Events blueprint with structure field, controller with future/past split, and 4 bilingual placeholder events ready for template rendering</done>
</task>

<task type="auto">
  <name>Task 2: Events template, event card snippet, and translation keys</name>
  <files>
    site/templates/events.php,
    site/snippets/sections/event-card.php,
    site/languages/it.php,
    site/languages/en.php
  </files>
  <read_first>
    site/templates/wines.php,
    site/snippets/sections/dish-card.php,
    site/controllers/site.php,
    site/languages/it.php,
    site/languages/en.php,
    src/css/main.css
  </read_first>
  <action>
1. Create `site/snippets/sections/event-card.php` per D-06, adapted from dish-card.php pattern:
   - Expects variables: $event (structure item), $isPast (boolean), $whatsapp (from site controller)
   - Card wrapper: `bg-dark/50 border border-light/10 rounded-lg overflow-hidden flex flex-col md:flex-row` + conditional `opacity-60` when $isPast is true (per D-07)
   - Image section: `md:w-1/3 h-48 md:h-auto` with object-cover, using $event->photo()->toFile() with thumb(['width' => 400, 'height' => 300, 'crop' => true, 'quality' => 80])
   - Content section: `p-6 flex-1 flex flex-col justify-between`
   - Date badge: `text-primary font-bold text-sm uppercase tracking-wider mb-2` showing date formatted as `d F Y` + time if present (e.g., "15 Maggio 2026 - ore 19:30")
   - Title: `font-heading text-xl text-light mb-3`
   - Description: `text-light/70 text-sm`
   - WhatsApp CTA (only for upcoming events with whatsapp_booking true): `btn-primary text-center mt-4`, href to `wa.me/{phone}?text={rawurlencode(t('event.whatsapp_message'))}`. Use str_replace(['+', ' '], '', $whatsapp) to clean phone number. Use rawurlencode() for message text per Pitfall 6 from RESEARCH.md.
   - WhatsApp message uses t() with replacements for event name and date

2. Create `site/templates/events.php` per D-07:
   - Standard template structure: snippet('layout/header'), main, snippet('layout/footer')
   - Page header: section-padding bg-dark, container-site text-center, h1 + subtitle (same as gallery/wines)

   - **Upcoming Events section** (if $upcoming->count() > 0):
     - Section heading: `text-xl font-heading text-primary mb-8` with t('events.upcoming')
     - Grid of event cards: `space-y-6` container
     - Loop: foreach $upcoming as $event, snippet('sections/event-card', ['event' => $event, 'isPast' => false, 'whatsapp' => $whatsapp])

   - **Empty state** (if $upcoming->count() === 0):
     - Centered container with t('events.empty.title') as heading and t('events.empty.body') as body text

   - **Past Events section** (if $past->count() > 0):
     - Divider: `border-t border-light/10 mt-12 pt-8` (per UI-SPEC)
     - Section heading: `text-xl font-heading text-light/50 mb-8` with t('events.past')
     - Loop: foreach $past as $event, snippet('sections/event-card', ['event' => $event, 'isPast' => true, 'whatsapp' => $whatsapp])

   NOTE: $whatsapp comes from the site.php global controller (already provides it to all templates). $upcoming and $past come from the events.php controller created in Task 1.

3. Add event translation keys to `site/languages/it.php` (append to translations array):
   - 'events.title' => 'Eventi'
   - 'events.subtitle' => 'Serate speciali, degustazioni e momenti da condividere'
   - 'events.upcoming' => 'Prossimi Eventi'
   - 'events.past' => 'Eventi Passati'
   - 'events.empty.title' => 'Nessun evento in programma'
   - 'events.empty.body' => 'Seguici sui social per restare aggiornato sulle prossime serate speciali.'
   - 'event.prenota' => 'Prenota su WhatsApp'
   - 'event.whatsapp_message' => 'Ciao! Vorrei prenotare per l\'evento "{event}" del {date}.'

4. Add event translation keys to `site/languages/en.php` (append to translations array):
   - 'events.title' => 'Events'
   - 'events.subtitle' => 'Special evenings, tastings, and moments to share'
   - 'events.upcoming' => 'Upcoming Events'
   - 'events.past' => 'Past Events'
   - 'events.empty.title' => 'No upcoming events'
   - 'events.empty.body' => 'Follow us on social media to stay updated on upcoming special evenings.'
   - 'event.prenota' => 'Book on WhatsApp'
   - 'event.whatsapp_message' => 'Hi! I\'d like to book for the event "{event}" on {date}.'

NOTE on WhatsApp message template: Kirby's t() function supports simple string replacement. Use str_replace(['{event}', '{date}'], [$event->title(), $event->event_date()->toDate('d/m/Y')], t('event.whatsapp_message')) in the event-card snippet.

IMPORTANT: Both plans 01 and 02 modify it.php and en.php. Since they run in wave 1 (parallel), the executor must handle merge carefully. The gallery keys and event keys are independent additions to the translations array -- append both sets. If running sequentially within the same executor instance, the second edit will see the first's additions.
  </action>
  <verify>
    <automated>php -r "echo file_exists('site/templates/events.php') && file_exists('site/snippets/sections/event-card.php') ? 'OK' : 'FAIL';" && grep -l "events.upcoming" site/languages/it.php && grep -l "events.upcoming" site/languages/en.php</automated>
  </verify>
  <acceptance_criteria>
    - site/templates/events.php exists with snippet('layout/header') and snippet('layout/footer')
    - Template renders $upcoming section with event cards and $past section with dimmed cards
    - Template shows empty state when no upcoming events
    - site/snippets/sections/event-card.php exists with card layout matching dish-card.php pattern
    - Event card shows date badge in text-primary, title, description, and conditional WhatsApp CTA
    - Past event cards have opacity-60 class
    - WhatsApp link uses rawurlencode() for message text
    - site/languages/it.php contains events.* and event.* translation keys
    - site/languages/en.php contains events.* and event.* translation keys
  </acceptance_criteria>
  <done>Events page renders upcoming events with WhatsApp booking and past events with dimmed styling, all UI strings translated IT/EN, event cards consistent with dish-card visual pattern</done>
</task>

</tasks>

<verification>
1. Visit /eventi -- page shows 3 upcoming event cards with photos, dates, descriptions, and "Prenota su WhatsApp" buttons
2. Scroll down -- "Eventi Passati" section shows Wine & Wonton Night card with dimmed opacity
3. Click "Prenota su WhatsApp" on an upcoming event -- WhatsApp opens with pre-filled message including event name and date
4. Switch to English (/en/events) -- all labels show English text, event descriptions in English
5. Past event card does NOT show booking button
6. Kirby Panel: events page shows structure field with 4 events, each editable with all fields
</verification>

<success_criteria>
- Events page renders at /eventi (IT) and /en/events (EN)
- 3 upcoming events with date, time, description, photo, and WhatsApp CTA (D-06, D-09)
- 1 past event in separate section with dimmed styling (D-07)
- Event cards visually consistent with dish-card.php pattern (D-06)
- WhatsApp booking links work with pre-filled message (D-09)
- All content editable from Kirby Panel via structure fields (D-12)
- All UI strings use t() translation system (D-10)
</success_criteria>

<output>
After completion, create `.planning/phases/03-engagement-features/03-02-SUMMARY.md`
</output>
