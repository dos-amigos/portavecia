---
phase: 03-engagement-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/blueprints/files/gallery-image.yml
  - site/blueprints/pages/gallery.yml
  - site/templates/gallery.php
  - src/js/main.js
  - content/6_gallery/gallery.it.txt
  - content/6_gallery/gallery.en.txt
autonomous: true
requirements:
  - GALL-01
  - GALL-02
  - GALL-03
must_haves:
  truths:
    - "Gallery page displays photos in a masonry grid layout"
    - "Clicking a filter button shows only photos of that category"
    - "Clicking a photo opens GLightbox with swipe navigation and photo counter"
    - "Lightbox only cycles through photos matching the active filter"
    - "Gallery photos have category and caption editable from Kirby Panel"
  artifacts:
    - path: "site/blueprints/files/gallery-image.yml"
      provides: "File blueprint with category select and caption fields"
      contains: "category"
    - path: "site/blueprints/pages/gallery.yml"
      provides: "Gallery page blueprint with files section"
      contains: "gallery-image"
    - path: "site/templates/gallery.php"
      provides: "Gallery page template with masonry grid, filters, GLightbox"
      min_lines: 40
    - path: "content/6_gallery/gallery.it.txt"
      provides: "Gallery page Italian content"
    - path: "content/6_gallery/gallery.en.txt"
      provides: "Gallery page English content"
  key_links:
    - from: "site/templates/gallery.php"
      to: "GLightbox CDN"
      via: "CDN script tag + elements array API"
      pattern: "glightbox"
    - from: "site/templates/gallery.php"
      to: "Alpine.js filter state"
      via: "x-data galleryFilter with x-show on photo items"
      pattern: "x-data.*galleryFilter"
    - from: "site/blueprints/pages/gallery.yml"
      to: "site/blueprints/files/gallery-image.yml"
      via: "template: gallery-image in files section"
      pattern: "template.*gallery-image"
---

<objective>
Build the Gallery page with masonry photo grid, category filter tabs (Alpine.js), and GLightbox lightbox -- all photos from _sources/ pre-populated with assigned categories, fully editable from Kirby Panel.

Purpose: Visitors can explore venue, food, and atmosphere photos with an elegant Pinterest-style layout, filter by category, and view full-size in a touch-friendly lightbox.
Output: Working gallery page at /galleria (IT) and /en/gallery (EN) with 16 photos, filter tabs, and lightbox.
</objective>

<execution_context>
@C:\Users\boxwe\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\boxwe\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-engagement-features/03-CONTEXT.md
@.planning/phases/03-engagement-features/03-RESEARCH.md
@.planning/phases/03-engagement-features/03-UI-SPEC.md

@site/templates/wines.php
@site/blueprints/pages/wines.yml
@site/snippets/sections/contact-map.php
@site/languages/it.php
@site/languages/en.php
@src/js/main.js

<interfaces>
<!-- Established patterns from Phase 1-2 that executor MUST follow -->

Template structure (from wines.php):
```php
<?php snippet('layout/header') ?>
<main>
  <section class="section-padding bg-dark">
    <div class="container-site text-center">
      <h1 class="font-heading text-4xl md:text-5xl text-primary mb-4"><?= $page->headline() ?></h1>
    </div>
  </section>
  <!-- content sections -->
</main>
<?php snippet('layout/footer') ?>
```

CDN loading pattern (from contact-map.php):
```html
<link rel="stylesheet" href="https://cdn-url/style.css" />
<script src="https://cdn-url/script.js"></script>
<script>document.addEventListener('DOMContentLoaded', function() { /* init */ });</script>
```

Card styling pattern: `bg-dark/50 border border-light/10 rounded-lg overflow-hidden`

Global data (from site.php controller): $phone, $whatsapp, $email, $address, $hours, $social
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gallery blueprint, file blueprint, and content setup</name>
  <files>
    site/blueprints/files/gallery-image.yml,
    site/blueprints/pages/gallery.yml,
    content/6_gallery/gallery.it.txt,
    content/6_gallery/gallery.en.txt
  </files>
  <read_first>
    site/blueprints/pages/wines.yml,
    site/languages/it.php,
    site/languages/en.php
  </read_first>
  <action>
1. Create file blueprint `site/blueprints/files/gallery-image.yml` per D-11:
   - title: "Foto Galleria"
   - accept mime: image/jpeg, image/png, image/webp
   - Fields: caption (text, translate: true), alt (text, translate: true), category (select, required: true, default: locale, options: locale/Locale, piatti/Piatti, vini/Vini, cucina/Cucina, eventi/Eventi)

2. Create page blueprint `site/blueprints/pages/gallery.yml`:
   - title: Galleria
   - Two tabs: "content" (headline text required + intro textarea small) and "photos" (files section with layout: cards, template: gallery-image, sortable: true, image cover: true ratio: 3/2, info: "{{ file.category }}")

3. Create content directory `content/6_gallery/` and content files:
   - `gallery.it.txt` with: Title: Galleria, Headline: Galleria, Intro: Scorci di atmosfera, piatti e momenti speciali
   - `gallery.en.txt` with: Title: Gallery, Headline: Gallery, Intro: Glimpses of atmosphere, dishes, and special moments

4. Copy all 16 photos from `_sources/` to `content/6_gallery/` (exclude logo-e-orari.jpeg and the .mp4 video -- only food/venue photos). For each copied photo, create corresponding `.it.txt` and `.en.txt` metadata files with category and caption fields per D-03:
   - **Locale category:** sala-interna.jpeg, sala-interna 2.jpeg, sala-interna 3.jpeg, sala-interna 2 tavolino per 2.jpeg, sala-interna-4-tavolino per 2.jpeg (5 photos)
   - **Piatti category:** menu-food.jpeg, menu-food_2.jpeg, Ravioli cinesi.jpeg, Spaghetti di riso con verdure e gamberi.jpeg, Pasta-wenzhounese.jpeg, piatto-pesce.jpeg, Polpetta di pesce wenzhounese.jpeg, Riso-wenzhounese.jpeg, Zuppa wonton wenzhounese.jpeg (9 photos)
   - **Cucina category:** cucina-preparazione-ingredienti.jpeg (1 photo)

   Each photo metadata file format (e.g., `sala-interna.jpeg.it.txt`):
   ```
   Caption: Sala interna
   Alt: Vista della sala interna di Porta Vecia
   Category: locale
   ```
   And corresponding `.en.txt` with English caption/alt. Use descriptive Italian/English captions for each photo.

IMPORTANT: The file blueprint must use `template: gallery-image` in the blueprint filename convention. Kirby maps file blueprints by the `template` value set in the files section.
  </action>
  <verify>
    <automated>php -r "echo file_exists('site/blueprints/files/gallery-image.yml') && file_exists('site/blueprints/pages/gallery.yml') && file_exists('content/6_gallery/gallery.it.txt') && file_exists('content/6_gallery/gallery.en.txt') ? 'OK' : 'FAIL';"</automated>
  </verify>
  <acceptance_criteria>
    - site/blueprints/files/gallery-image.yml exists with category select field (options: locale, piatti, vini, cucina, eventi)
    - site/blueprints/pages/gallery.yml exists with files section using template: gallery-image
    - content/6_gallery/gallery.it.txt and gallery.en.txt exist with Headline and Intro fields
    - 15 photos copied from _sources/ to content/6_gallery/ (excluding logo-e-orari.jpeg and .mp4)
    - Each photo has .it.txt and .en.txt metadata files with Category, Caption, Alt fields
  </acceptance_criteria>
  <done>Gallery blueprint with file metadata fields exists, gallery content directory populated with 15 categorized photos and bilingual content files</done>
</task>

<task type="auto">
  <name>Task 2: Gallery template with masonry grid, Alpine.js filters, and GLightbox</name>
  <files>
    site/templates/gallery.php,
    src/js/main.js,
    site/languages/it.php,
    site/languages/en.php
  </files>
  <read_first>
    site/templates/wines.php,
    site/snippets/sections/contact-map.php,
    src/js/main.js,
    site/languages/it.php,
    site/languages/en.php,
    src/css/main.css
  </read_first>
  <action>
1. Create `site/templates/gallery.php` following the established template structure (snippet header/footer):

   **Page header section** (same pattern as wines.php):
   - section-padding bg-dark, container-site text-center
   - h1 with font-heading text-4xl md:text-5xl text-primary (use $page->headline())
   - Subtitle p with text-light/70 text-lg max-w-2xl mx-auto (use $page->intro())

   **Filter bar** per D-02:
   - Centered horizontal flex row with gap-2, wrapped in container-site, mb-8
   - Alpine.js x-data using galleryFilter() function
   - Filter buttons for: Tutto, Locale, Piatti, Vini, Cucina, Eventi
   - Each button uses t() for label (e.g., t('gallery.filter.all'))
   - Active state: `px-4 py-2 text-sm font-bold text-dark bg-primary rounded-full uppercase tracking-wider`
   - Inactive state: `px-4 py-2 text-sm font-bold text-light/70 bg-dark/50 border border-light/10 rounded-full uppercase tracking-wider cursor-pointer transition-colors hover:text-light`
   - Button click calls setFilter('category') method
   - Use x-bind:class to toggle active/inactive based on activeFilter === 'category'

   **Masonry grid** per D-01:
   - CSS columns: `columns-2 sm:columns-3 lg:columns-4 gap-3`
   - Loop through $page->images()->sortBy('sort') as $image
   - Each photo wrapped in div with `break-inside-avoid mb-3`
   - Use x-show with Alpine.js: `x-show="activeFilter === 'all' || '<?= $image->category()->value() ?: 'locale' ?>' === activeFilter"`
   - Add x-transition: `x-transition:enter="transition ease-out duration-200"` + `x-transition:enter-start="opacity-0"` + `x-transition:enter-end="opacity-100"`
   - Photo link: `<a>` tag with class `glightbox block rounded-lg overflow-hidden group relative cursor-pointer`
   - data-category attribute: `data-category="<?= $image->category()->value() ?: 'locale' ?>"`
   - data-title attribute: `<?= $image->caption() ?>`
   - Image src: use `$image->thumb(['width' => 600, 'quality' => 80])->url()` for grid thumbnail
   - href: use `$image->url()` for full-size lightbox view
   - Image classes: `w-full transition-transform duration-300 group-hover:scale-105` with loading="lazy"
   - Hover overlay: `absolute inset-0 bg-dark/40 opacity-0 group-hover:opacity-100 transition-opacity flex items-end p-4` with caption span in text-light text-sm

   **GLightbox CDN** (follow contact-map.php pattern):
   - Load GLightbox CSS in head area of template (before grid): `<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/glightbox/3.3.1/css/glightbox.min.css" />`
   - Load GLightbox JS after grid: `<script src="https://cdnjs.cloudflare.com/ajax/libs/glightbox/3.3.1/js/glightbox.min.js"></script>`

   **Empty state**: if $page->images()->count() === 0, show centered text with t('gallery.empty')

2. Add galleryFilter() function to `src/js/main.js` (append, do not overwrite existing code):
   ```javascript
   // Gallery filter + GLightbox integration
   window.galleryFilter = function() {
     return {
       activeFilter: 'all',
       lightbox: null,

       init() {
         this.$nextTick(() => this.buildLightbox());
       },

       setFilter(filter) {
         this.activeFilter = filter;
         this.$nextTick(() => {
           setTimeout(() => this.buildLightbox(), 250);
         });
       },

       buildLightbox() {
         if (this.lightbox) {
           this.lightbox.destroy();
         }

         const container = this.$el.querySelector('.gallery-grid');
         if (!container) return;

         const visibleItems = container.querySelectorAll('.glightbox');
         const elements = [];
         const clickTargets = [];

         visibleItems.forEach(el => {
           if (el.offsetParent !== null) {
             elements.push({
               href: el.getAttribute('href'),
               type: 'image',
               title: el.getAttribute('data-title') || '',
             });
             clickTargets.push(el);
           }
         });

         if (elements.length === 0) return;

         this.lightbox = GLightbox({ elements, loop: true, zoomable: true, touchNavigation: true });

         clickTargets.forEach((el, i) => {
           el.onclick = (e) => {
             e.preventDefault();
             this.lightbox.openAt(i);
           };
         });
       }
     };
   };
   ```

   Per D-05, this ensures lightbox only cycles through photos matching the active filter.

3. Add gallery translation keys to `site/languages/it.php` (append to translations array):
   - 'gallery.title' => 'Galleria'
   - 'gallery.subtitle' => 'Scorci di atmosfera, piatti e momenti speciali'
   - 'gallery.filter.all' => 'Tutto'
   - 'gallery.filter.locale' => 'Locale'
   - 'gallery.filter.piatti' => 'Piatti'
   - 'gallery.filter.vini' => 'Vini'
   - 'gallery.filter.cucina' => 'Cucina'
   - 'gallery.filter.eventi' => 'Eventi'
   - 'gallery.empty' => 'La galleria fotografica e in arrivo. Torna presto!'

4. Add gallery translation keys to `site/languages/en.php` (append to translations array):
   - 'gallery.title' => 'Gallery'
   - 'gallery.subtitle' => 'Glimpses of atmosphere, dishes, and special moments'
   - 'gallery.filter.all' => 'All'
   - 'gallery.filter.locale' => 'Venue'
   - 'gallery.filter.piatti' => 'Dishes'
   - 'gallery.filter.vini' => 'Wines'
   - 'gallery.filter.cucina' => 'Kitchen'
   - 'gallery.filter.eventi' => 'Events'
   - 'gallery.empty' => 'Our photo gallery is coming soon. Check back later!'

IMPORTANT: Do NOT use Tailwind arbitrary values like bg-[#hex] -- they don't compile. Use theme tokens (bg-dark, text-primary, etc.) and inline styles for non-theme values. The hover overlay uses bg-dark/40 which is valid (opacity modifier on theme color).
  </action>
  <verify>
    <automated>php -r "echo file_exists('site/templates/gallery.php') ? 'OK' : 'FAIL';" && grep -l "galleryFilter" src/js/main.js && grep -l "gallery.filter.all" site/languages/it.php && grep -l "gallery.filter.all" site/languages/en.php</automated>
  </verify>
  <acceptance_criteria>
    - site/templates/gallery.php exists with snippet('layout/header') and snippet('layout/footer')
    - Template contains CSS columns masonry: columns-2 sm:columns-3 lg:columns-4
    - Template contains Alpine.js x-data="galleryFilter()" for filter state
    - Template contains 6 filter buttons (Tutto, Locale, Piatti, Vini, Cucina, Eventi) using t() keys
    - Template loads GLightbox 3.3.1 CSS and JS via CDN
    - Template uses $image->thumb() for grid thumbnails and $image->url() for lightbox href
    - src/js/main.js contains galleryFilter() function with buildLightbox() that rebuilds from visible elements
    - site/languages/it.php contains gallery.filter.* keys
    - site/languages/en.php contains gallery.filter.* keys
  </acceptance_criteria>
  <done>Gallery page renders masonry grid with working category filters, GLightbox opens with only filtered photos, all UI strings translated IT/EN</done>
</task>

</tasks>

<verification>
1. Visit /galleria -- page shows masonry grid of 15 photos with filter bar
2. Click "Piatti" filter -- only food photos visible, grid reflows
3. Click a photo -- GLightbox opens with counter showing only piatti count
4. Swipe/arrow through lightbox -- only piatti photos cycle
5. Click "Tutto" -- all photos visible again
6. Switch language to English -- filter labels show English text (All, Venue, Dishes, etc.)
7. Kirby Panel: gallery page shows photos in cards layout with category metadata visible
</verification>

<success_criteria>
- Gallery page renders at /galleria (IT) and /en/gallery (EN)
- Masonry layout with 2/3/4 columns responsive
- 6 filter tabs work with Alpine.js transitions
- GLightbox lightbox respects active filter (D-05)
- 15 photos displayed with correct categories (D-03)
- All UI strings use t() translation system (D-10)
- Photos editable from Panel with category, caption, alt fields (D-11)
</success_criteria>

<output>
After completion, create `.planning/phases/03-engagement-features/03-01-SUMMARY.md`
</output>
