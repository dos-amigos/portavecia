---
phase: 02-core-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - site/blueprints/fields/dishes.yml
  - site/blueprints/pages/menu.yml
  - site/templates/menu.php
  - site/snippets/sections/dish-card.php
  - content/3_menu/menu.it.txt
  - content/3_menu/menu.en.txt
autonomous: true
requirements: [MENU-01, MENU-02, MENU-03]

must_haves:
  truths:
    - "Menu page lists dishes organized by category (Antipasti, Primi, Secondi, Zuppe, Dolci)"
    - "Each dish shows as a card with photo, name, description, and price"
    - "Main dishes display wine pairing suggestions"
    - "The bar owner can add, remove, and edit dishes from Kirby Panel"
    - "Menu content is available in both Italian and English"
  artifacts:
    - path: "site/blueprints/fields/dishes.yml"
      provides: "Reusable dish structure field definition"
      contains: "type: structure"
    - path: "site/blueprints/pages/menu.yml"
      provides: "Menu page blueprint with category tabs"
      contains: "extends: fields/dishes"
    - path: "site/templates/menu.php"
      provides: "Menu page template rendering dish cards by category"
      contains: "snippet('sections/dish-card'"
    - path: "site/snippets/sections/dish-card.php"
      provides: "Single dish card component"
      contains: "wine_pairing"
  key_links:
    - from: "site/templates/menu.php"
      to: "site/snippets/sections/dish-card.php"
      via: "snippet() include in category loop"
      pattern: "snippet\\('sections/dish-card'"
    - from: "site/blueprints/pages/menu.yml"
      to: "site/blueprints/fields/dishes.yml"
      via: "extends reference"
      pattern: "extends: fields/dishes"
---

<objective>
Build the Menu/Cucina page with dish cards organized by category, each showing photo, name, description, price, and optional wine pairing.

Purpose: The menu page is the primary content page showcasing authentic homemade Chinese cuisine. It must be visually appealing (card layout with photos per D-05) and fully editable from Kirby Panel (D-12).
Output: Menu blueprint with reusable dish field definitions, template, dish card snippet, and bilingual content files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-pages/02-CONTEXT.md
@.planning/phases/02-core-pages/02-RESEARCH.md

<interfaces>
From site/controllers/site.php (global data available in all templates):
```php
$phone, $whatsapp, $email, $address, $hours, $social
```

CSS classes available:
```css
.btn-primary, .btn-outline, .section-padding, .container-site
```

Theme tokens: --color-primary (gold), --color-dark, --color-light
Fonts: --font-heading (Playfair Display), --font-body (Raleway)

Template pattern:
```php
<?php snippet('layout/header') ?>
<main>...</main>
<?php snippet('layout/footer') ?>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reusable dish fields, menu blueprint, and content files</name>
  <files>
    site/blueprints/fields/dishes.yml,
    site/blueprints/pages/menu.yml,
    content/3_menu/menu.it.txt,
    content/3_menu/menu.en.txt
  </files>
  <read_first>
    site/blueprints/site.yml,
    site/blueprints/pages/home.yml,
    .planning/phases/02-core-pages/02-RESEARCH.md
  </read_first>
  <action>
**Create `site/blueprints/fields/` directory** if it does not exist.

**Create `site/blueprints/fields/dishes.yml`** -- reusable dish structure field definition per RESEARCH.md Pattern 1:
```yaml
label: Piatti
type: structure
sortable: true
fields:
  dish_name:
    label: Nome piatto
    type: text
    width: 1/4
    required: true
  description:
    label: Descrizione
    type: textarea
    size: small
    width: 1/4
  price:
    label: Prezzo
    type: text
    width: 1/6
    help: "Es: 8.00"
  photo:
    label: Foto
    type: files
    query: page.images
    multiple: false
    width: 1/6
  wine_pairing:
    label: Abbinamento vino
    type: text
    width: 1/6
```

**Create `site/blueprints/pages/menu.yml`** with category tabs per D-05:
```yaml
title: Menu
tabs:
  content:
    label: Contenuto
    fields:
      headline:
        label: Titolo pagina
        type: text
      intro:
        label: Introduzione
        type: textarea
        size: small
  images:
    label: Immagini
    sections:
      menu_images:
        label: Foto piatti
        type: files
        layout: cards
        image:
          ratio: 1/1
          cover: true
  antipasti:
    label: Antipasti
    fields:
      antipasti:
        extends: fields/dishes
        label: Antipasti
  primi:
    label: Primi
    fields:
      primi:
        extends: fields/dishes
        label: Primi Piatti
  secondi:
    label: Secondi
    fields:
      secondi:
        extends: fields/dishes
        label: Secondi Piatti
  zuppe:
    label: Zuppe
    fields:
      zuppe:
        extends: fields/dishes
        label: Zuppe
  dolci:
    label: Dolci
    fields:
      dolci:
        extends: fields/dishes
        label: Dolci
```

**Create `content/3_menu/` directory** and bilingual content files per D-11.

**`content/3_menu/menu.it.txt`:**
```
Title: La Nostra Cucina
----
Headline: Cucina Cinese Fatta in Casa
----
Intro: Ogni piatto nasce dalle mani esperte della nostra cuoca, con ingredienti freschi selezionati ogni giorno. La tradizione cinese incontra la qualita italiana in ricette autentiche, preparate al momento per voi.
----
Antipasti:
-
  dish_name: Involtini Primavera
  description: Croccanti involtini ripieni di verdure fresche, serviti con salsa agrodolce fatta in casa
  price: "6.00"
  wine_pairing: Prosecco DOC Treviso
-
  dish_name: Ravioli Cinesi al Vapore
  description: Delicati ravioli ripieni di carne di maiale e verdure, cotti al vapore secondo tradizione
  price: "8.00"
  wine_pairing: Soave Classico DOC
-
  dish_name: Edamame
  description: Fagioli di soia al vapore con sale marino e pepe di Sichuan
  price: "5.00"
  wine_pairing:
----
Primi:
-
  dish_name: Spaghetti di Riso con Verdure e Gamberi
  description: Spaghetti di riso saltati nel wok con gamberi freschi, verdure di stagione e salsa di soia
  price: "12.00"
  wine_pairing: Lugana DOC
-
  dish_name: Noodles con Manzo e Peperoni
  description: Noodles di frumento saltati con straccetti di manzo, peperoni e salsa piccante
  price: "11.00"
  wine_pairing: Valpolicella Ripasso DOC
-
  dish_name: Riso alla Cantonese
  description: Riso saltato con uova, prosciutto, piselli e cipollotto
  price: "9.00"
  wine_pairing: Pinot Grigio DOC
----
Secondi:
-
  dish_name: Polpette al Vapore
  description: Polpette di carne speziata cotte al vapore, accompagnate da salsa di soia e zenzero
  price: "10.00"
  wine_pairing: Bardolino DOC
-
  dish_name: Piatto di Pesce al Vapore
  description: Filetto di pesce fresco al vapore con zenzero, cipollotto e salsa di soia leggera
  price: "14.00"
  wine_pairing: Verdicchio dei Castelli di Jesi
----
Zuppe:
-
  dish_name: Zuppa di Wonton
  description: Brodo profumato con ravioli wonton ripieni di carne, cipollotto e sesamo
  price: "7.00"
  wine_pairing:
----
Dolci:
-
  dish_name: Riso Glutinoso con Mango
  description: Dolce tradizionale con riso glutinoso, latte di cocco e mango fresco
  price: "6.00"
  wine_pairing: Moscato d'Asti DOCG
```

**`content/3_menu/menu.en.txt`:** Same structure, English translations:
- Title: Our Kitchen
- Headline: Homemade Chinese Cuisine
- Intro: (English version of intro)
- All dish names, descriptions, and wine pairings translated to English
- Prices remain the same
  </action>
  <verify>
    <automated>test -f site/blueprints/fields/dishes.yml && grep -q "extends: fields/dishes" site/blueprints/pages/menu.yml && grep -q "wine_pairing" site/blueprints/fields/dishes.yml && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - site/blueprints/fields/dishes.yml exists and contains `type: structure`
    - site/blueprints/fields/dishes.yml contains `dish_name:`, `description:`, `price:`, `photo:`, `wine_pairing:` fields
    - site/blueprints/pages/menu.yml contains at least 5 category tabs (antipasti, primi, secondi, zuppe, dolci)
    - site/blueprints/pages/menu.yml contains `extends: fields/dishes` in each category tab
    - site/blueprints/pages/menu.yml contains a `content` tab with `headline:` and `intro:` fields
    - site/blueprints/pages/menu.yml contains an `images` section with `type: files`
    - content/3_menu/menu.it.txt contains `Headline: Cucina Cinese Fatta in Casa`
    - content/3_menu/menu.en.txt contains `Headline: Homemade Chinese Cuisine`
    - Both content files contain at least 8 dish entries across categories
  </acceptance_criteria>
  <done>Reusable dish field definition, menu blueprint with 5 category tabs, and bilingual content files with realistic placeholder dishes and wine pairings.</done>
</task>

<task type="auto">
  <name>Task 2: Menu template and dish card snippet</name>
  <files>
    site/templates/menu.php,
    site/snippets/sections/dish-card.php
  </files>
  <read_first>
    site/blueprints/pages/menu.yml,
    site/blueprints/fields/dishes.yml,
    site/templates/home.php,
    site/snippets/layout/header.php,
    src/css/main.css,
    .planning/phases/02-core-pages/02-RESEARCH.md
  </read_first>
  <action>
**Create `site/templates/menu.php`** per D-05:

```php
<?php snippet('layout/header') ?>
<main>
  <!-- Page header -->
  <section class="section-padding bg-dark">
    <div class="container-site text-center">
      <h1 class="font-heading text-4xl md:text-5xl text-primary mb-4"><?= $page->headline() ?></h1>
      <?php if ($page->intro()->isNotEmpty()): ?>
        <p class="text-light/70 text-lg max-w-2xl mx-auto"><?= $page->intro() ?></p>
      <?php endif ?>
    </div>
  </section>

  <?php
  $categories = [
    'antipasti' => t('menu.antipasti'),
    'primi' => t('menu.primi'),
    'secondi' => t('menu.secondi'),
    'zuppe' => t('menu.zuppe'),
    'dolci' => t('menu.dolci'),
  ];
  foreach ($categories as $cat => $label):
    $dishes = $page->$cat()->toStructure();
    if ($dishes->count()):
  ?>
  <section class="section-padding">
    <div class="container-site">
      <h2 class="font-heading text-3xl text-primary mb-8"><?= $label ?></h2>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <?php foreach ($dishes as $dish): ?>
          <?php snippet('sections/dish-card', ['dish' => $dish]) ?>
        <?php endforeach ?>
      </div>
    </div>
  </section>
  <?php
    endif;
  endforeach;
  ?>
</main>
<?php snippet('layout/footer') ?>
```

**Create `site/snippets/sections/dish-card.php`** -- card layout per D-05 with photo, name, description, price, and wine pairing per D-06 (Claude's discretion: show wine pairing as a small italic text below description with a wine glass icon):

```php
<div class="bg-dark/50 border border-light/10 rounded-lg overflow-hidden flex flex-col md:flex-row">
  <?php if ($photo = $dish->photo()->toFile()): ?>
    <div class="md:w-1/3 h-48 md:h-auto">
      <img src="<?= $photo->thumb(['width' => 400, 'height' => 400, 'crop' => true, 'quality' => 80])->url() ?>"
           alt="<?= $dish->dish_name() ?>"
           class="w-full h-full object-cover"
           loading="lazy">
    </div>
  <?php endif ?>
  <div class="p-6 flex-1 flex flex-col justify-between">
    <div>
      <div class="flex justify-between items-start mb-2">
        <h3 class="font-heading text-xl text-light"><?= $dish->dish_name() ?></h3>
        <?php if ($dish->price()->isNotEmpty()): ?>
          <span class="text-primary font-semibold text-lg ml-4 whitespace-nowrap">&euro; <?= $dish->price() ?></span>
        <?php endif ?>
      </div>
      <?php if ($dish->description()->isNotEmpty()): ?>
        <p class="text-light/70 text-sm mb-3"><?= $dish->description() ?></p>
      <?php endif ?>
    </div>
    <?php if ($dish->wine_pairing()->isNotEmpty()): ?>
      <p class="text-primary/70 text-xs italic mt-2">
        <span class="inline-block mr-1">&#127863;</span>
        <?= $dish->wine_pairing() ?>
      </p>
    <?php endif ?>
  </div>
</div>
```

The wine pairing (D-06, Claude's discretion) appears as a subtle italic line at the bottom of the card with a wine glass emoji, only when the field is not empty.
  </action>
  <verify>
    <automated>grep -q "snippet('sections/dish-card'" site/templates/menu.php && grep -q "toStructure" site/templates/menu.php && grep -q "wine_pairing" site/snippets/sections/dish-card.php && grep -q "dish_name" site/snippets/sections/dish-card.php && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - site/templates/menu.php contains `snippet('layout/header')` and `snippet('layout/footer')`
    - site/templates/menu.php iterates over category keys: antipasti, primi, secondi, zuppe, dolci
    - site/templates/menu.php calls `$page->$cat()->toStructure()` for each category
    - site/templates/menu.php calls `snippet('sections/dish-card', ['dish' => $dish])`
    - site/templates/menu.php uses `t('menu.antipasti')` (or equivalent t() calls) for category headings
    - site/snippets/sections/dish-card.php renders `$dish->dish_name()`, `$dish->description()`, `$dish->price()`
    - site/snippets/sections/dish-card.php renders `$dish->photo()->toFile()` with thumb()
    - site/snippets/sections/dish-card.php conditionally renders `$dish->wine_pairing()` when not empty
  </acceptance_criteria>
  <done>Menu page renders dishes as photo cards organized by category, with name, description, price, and optional wine pairing. Category headings use translation keys. Layout is responsive (single column mobile, two columns desktop).</done>
</task>

</tasks>

<verification>
- Menu page loads with page header (headline + intro)
- Dishes display as cards with photos in a grid layout
- Each card shows name, description, price, and wine pairing (when present)
- Categories (Antipasti, Primi, Secondi, Zuppe, Dolci) each have their own section
- Blueprint loads in Kirby Panel with category tabs
- Content available in both IT and EN
</verification>

<success_criteria>
- Menu page renders all dish categories with card-style layout
- Wine pairing suggestions display on dishes that have them
- Owner can add/remove/edit dishes per category from Kirby Panel tabs
- Bilingual content files provide IT and EN versions
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pages/02-02-SUMMARY.md`
</output>
