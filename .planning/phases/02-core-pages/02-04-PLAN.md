---
phase: 02-core-pages
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - site/blueprints/pages/about.yml
  - site/templates/about.php
  - site/snippets/sections/about-block.php
  - site/blueprints/pages/contact.yml
  - site/templates/contact.php
  - site/snippets/sections/contact-map.php
  - content/2_about/about.it.txt
  - content/2_about/about.en.txt
  - content/5_contact/contact.it.txt
  - content/5_contact/contact.en.txt
autonomous: true
requirements: [ABOUT-01, ABOUT-02, CONT-01, CONT-02, CONT-03, CONT-04]

must_haves:
  truths:
    - "About page presents the story, philosophy, interior photos, and fusion concept in alternating text/image blocks"
    - "About page includes a dedicated 'Due Tradizioni, Un Tavolo' fusion concept section"
    - "Contact page shows an OpenStreetMap map with a marker on Porta Vecia location"
    - "Contact page displays opening hours clearly visible"
    - "Contact page has one-tap WhatsApp and phone buttons"
    - "Contact page shows full address with an 'Indicazioni' button linking to Google Maps"
    - "All content available in Italian and English"
  artifacts:
    - path: "site/blueprints/pages/about.yml"
      provides: "About page blueprint with alternating block fields"
      contains: "story_title"
    - path: "site/templates/about.php"
      provides: "About page template with alternating blocks"
      contains: "snippet('sections/about-block'"
    - path: "site/blueprints/pages/contact.yml"
      provides: "Contact page blueprint"
      contains: "map_lat"
    - path: "site/templates/contact.php"
      provides: "Contact page template with map and info"
      contains: "snippet('sections/contact-map'"
    - path: "site/snippets/sections/contact-map.php"
      provides: "Leaflet OpenStreetMap embed"
      contains: "openstreetmap.de"
  key_links:
    - from: "site/templates/about.php"
      to: "site/snippets/sections/about-block.php"
      via: "snippet() include in block loop"
      pattern: "snippet\\('sections/about-block'"
    - from: "site/templates/contact.php"
      to: "site/snippets/sections/contact-map.php"
      via: "snippet() include"
      pattern: "snippet\\('sections/contact-map'"
    - from: "site/snippets/sections/contact-map.php"
      to: "Leaflet CDN"
      via: "script/link tags"
      pattern: "unpkg.com/leaflet"
---

<objective>
Build the Chi Siamo (About) page with alternating storytelling blocks and the Contatti (Contact) page with OpenStreetMap, opening hours, WhatsApp/phone buttons, and address.

Purpose: Chi Siamo tells the Porta Vecia story and fusion philosophy. Contatti makes it easy to find and reach the venue. Both are essential information pages that complete the core site.
Output: Blueprints, templates, snippets, and bilingual content files for both pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-pages/02-CONTEXT.md
@.planning/phases/02-core-pages/02-RESEARCH.md

<interfaces>
From site/controllers/site.php (global data available):
```php
$phone, $whatsapp, $email, $address, $hours, $social
```

CSS classes: .btn-primary, .btn-outline, .section-padding, .container-site
Theme: --color-primary (gold), --color-dark, --color-light
Fonts: --font-heading (Playfair Display), --font-body (Raleway)

Template pattern:
```php
<?php snippet('layout/header') ?>
<main>...</main>
<?php snippet('layout/footer') ?>
```

Leaflet CDN (load only on contact page):
```html
<link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" crossorigin="" />
<script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js" crossorigin=""></script>
```

Tile server: openstreetmap.de (German-hosted, GDPR-safe)
Approximate coordinates: 45.2272, 11.6574 (Este center)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: About page (blueprint, template, snippet, content)</name>
  <files>
    site/blueprints/pages/about.yml,
    site/templates/about.php,
    site/snippets/sections/about-block.php,
    content/2_about/about.it.txt,
    content/2_about/about.en.txt
  </files>
  <read_first>
    site/blueprints/site.yml,
    site/templates/home.php,
    site/snippets/layout/header.php,
    src/css/main.css,
    .planning/phases/02-core-pages/02-RESEARCH.md
  </read_first>
  <action>
**Create `site/blueprints/pages/about.yml`** per D-10 (alternating storytelling blocks -- 4 blocks: story, place, fusion, atmosphere):

```yaml
title: Chi Siamo
tabs:
  content:
    label: Contenuto
    fields:
      headline:
        label: Titolo pagina
        type: text
        required: true
      intro:
        label: Introduzione
        type: textarea
        size: small
  story:
    label: La Storia
    fields:
      story_title:
        label: Titolo
        type: text
      story_text:
        label: Testo
        type: textarea
      story_image:
        label: Immagine
        type: files
        query: page.images
        multiple: false
  place:
    label: Il Locale
    fields:
      place_title:
        label: Titolo
        type: text
      place_text:
        label: Testo
        type: textarea
      place_image:
        label: Immagine
        type: files
        query: page.images
        multiple: false
  fusion:
    label: Due Tradizioni
    fields:
      fusion_title:
        label: Titolo
        type: text
      fusion_text:
        label: Testo
        type: textarea
      fusion_image:
        label: Immagine
        type: files
        query: page.images
        multiple: false
  atmosphere:
    label: L'Atmosfera
    fields:
      atmosphere_title:
        label: Titolo
        type: text
      atmosphere_text:
        label: Testo
        type: textarea
      atmosphere_image:
        label: Immagine
        type: files
        query: page.images
        multiple: false
  images:
    label: Immagini
    sections:
      about_images:
        label: Foto
        type: files
        layout: cards
        image:
          ratio: 3/2
          cover: true
```

**Create `site/templates/about.php`** per D-10 (alternating text/image blocks):

```php
<?php snippet('layout/header') ?>
<main>
  <!-- Page header -->
  <section class="section-padding bg-dark">
    <div class="container-site text-center">
      <h1 class="font-heading text-4xl md:text-5xl text-primary mb-4"><?= $page->headline() ?></h1>
      <?php if ($page->intro()->isNotEmpty()): ?>
        <p class="text-light/70 text-lg max-w-2xl mx-auto"><?= $page->intro() ?></p>
      <?php endif ?>
    </div>
  </section>

  <?php
  // Alternating storytelling blocks per D-10
  $blocks = [
    ['title' => 'story_title', 'text' => 'story_text', 'image' => 'story_image'],
    ['title' => 'place_title', 'text' => 'place_text', 'image' => 'place_image'],
    ['title' => 'fusion_title', 'text' => 'fusion_text', 'image' => 'fusion_image'],
    ['title' => 'atmosphere_title', 'text' => 'atmosphere_text', 'image' => 'atmosphere_image'],
  ];
  foreach ($blocks as $i => $block):
    snippet('sections/about-block', [
      'page' => $page,
      'block' => $block,
      'reverse' => ($i % 2 !== 0),
    ]);
  endforeach;
  ?>
</main>
<?php snippet('layout/footer') ?>
```

**Create `site/snippets/sections/about-block.php`** per RESEARCH.md alternating block pattern:

```php
<?php
$title = $page->{$block['title']}();
$text = $page->{$block['text']}();
$img = $page->{$block['image']}()->toFile();
if ($title->isEmpty() && $text->isEmpty()) return;
?>
<section class="section-padding">
  <div class="container-site grid grid-cols-1 md:grid-cols-2 gap-12 items-center">
    <div class="<?= $reverse ? 'md:order-2' : '' ?>">
      <h2 class="font-heading text-3xl text-primary mb-6"><?= $title ?></h2>
      <div class="prose prose-invert">
        <?= $text->kirbytext() ?>
      </div>
    </div>
    <div class="<?= $reverse ? 'md:order-1' : '' ?>">
      <?php if ($img): ?>
        <img src="<?= $img->thumb(['width' => 800, 'quality' => 80])->url() ?>"
             alt="<?= $img->alt()->or($title) ?>"
             class="rounded-lg w-full shadow-lg"
             loading="lazy">
      <?php endif ?>
    </div>
  </div>
</section>
```

**Create `content/2_about/` directory** and bilingual content files per D-11:

**`content/2_about/about.it.txt`:**
- Title: Chi Siamo
- Headline: La Nostra Storia
- Intro: Nel cuore del centro storico di Este, dove le antiche mura raccontano secoli di storia...
- story_title: Come e Nata Porta Vecia
- story_text: (3-4 sentences about how the enoteca started, the idea of combining Italian wine culture with authentic Chinese cuisine, the passion for quality)
- place_title: Il Locale
- place_text: (3-4 sentences about the venue itself -- intimate, historic setting, the plateatico, the warm atmosphere)
- fusion_title: Due Tradizioni, Un Tavolo (per D-10/ABOUT-02)
- fusion_text: (3-4 sentences about the fusion concept -- how homemade Chinese cuisine pairs beautifully with Italian wines, breaking conventions while respecting both traditions)
- atmosphere_title: L'Atmosfera
- atmosphere_text: (3-4 sentences about the experience -- the aperitivo culture, the convivial atmosphere, the evening dining experience)

**`content/2_about/about.en.txt`:** English translations of all fields.
  </action>
  <verify>
    <automated>grep -q "story_title" site/blueprints/pages/about.yml && grep -q "fusion_title" site/blueprints/pages/about.yml && grep -q "about-block" site/templates/about.php && grep -q "md:order-2" site/snippets/sections/about-block.php && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - site/blueprints/pages/about.yml contains tabs: content, story, place, fusion, atmosphere, images
    - site/blueprints/pages/about.yml contains fields: story_title, story_text, story_image (and equivalent for place, fusion, atmosphere)
    - site/templates/about.php iterates over 4 blocks calling `snippet('sections/about-block'`
    - site/templates/about.php passes `'reverse' => ($i % 2 !== 0)` to the snippet
    - site/snippets/sections/about-block.php uses `md:order-2` and `md:order-1` for alternating layout
    - site/snippets/sections/about-block.php renders title, text (kirbytext), and image (thumb)
    - content/2_about/about.it.txt contains `Fusion_title: Due Tradizioni, Un Tavolo`
    - content/2_about/about.en.txt exists with English translations
  </acceptance_criteria>
  <done>About page renders 4 alternating storytelling blocks (story, place, fusion concept, atmosphere) with text/image sides swapping. Includes the "Due Tradizioni, Un Tavolo" fusion section per ABOUT-02. Fully editable from Panel.</done>
</task>

<task type="auto">
  <name>Task 2: Contact page (blueprint, template, map snippet, content)</name>
  <files>
    site/blueprints/pages/contact.yml,
    site/templates/contact.php,
    site/snippets/sections/contact-map.php,
    content/5_contact/contact.it.txt,
    content/5_contact/contact.en.txt
  </files>
  <read_first>
    site/blueprints/site.yml,
    site/controllers/site.php,
    site/snippets/layout/header.php,
    site/snippets/layout/footer.php,
    src/css/main.css,
    .planning/phases/02-core-pages/02-RESEARCH.md
  </read_first>
  <action>
**Create `site/blueprints/pages/contact.yml`** per D-08, D-09:

```yaml
title: Contatti
fields:
  headline:
    label: Titolo pagina
    type: text
    required: true
  intro:
    label: Introduzione
    type: textarea
    size: small
  map_lat:
    label: Latitudine
    type: text
    width: 1/2
    help: "Es: 45.2272"
    translate: false
  map_lng:
    label: Longitudine
    type: text
    width: 1/2
    help: "Es: 11.6574"
    translate: false
  map_popup:
    label: Testo popup mappa
    type: text
  directions_url:
    label: URL Indicazioni (Google Maps)
    type: url
    translate: false
    help: "Link a Google Maps con le coordinate del locale"
```

Note: Contact info (phone, whatsapp, email, address, hours) comes from the global site.yml blueprint via site.php controller. No need to duplicate these fields.

**Create `site/templates/contact.php`** per D-09 (split layout: map left, info right, stacks on mobile):

```php
<?php snippet('layout/header') ?>
<main>
  <!-- Page header -->
  <section class="section-padding bg-dark">
    <div class="container-site text-center">
      <h1 class="font-heading text-4xl md:text-5xl text-primary mb-4"><?= $page->headline() ?></h1>
      <?php if ($page->intro()->isNotEmpty()): ?>
        <p class="text-light/70 text-lg max-w-2xl mx-auto"><?= $page->intro() ?></p>
      <?php endif ?>
    </div>
  </section>

  <!-- Split layout: map left, info right -->
  <section class="section-padding">
    <div class="container-site grid grid-cols-1 md:grid-cols-2 gap-12">
      <!-- Map (left) per D-08 -->
      <div>
        <?php snippet('sections/contact-map', [
          'lat' => $page->map_lat()->or('45.2272')->value(),
          'lng' => $page->map_lng()->or('11.6574')->value(),
          'popup' => $page->map_popup()->or('Porta Vecia')->value(),
        ]) ?>
      </div>

      <!-- Contact info (right) -->
      <div class="space-y-8">
        <!-- Address per CONT-04 -->
        <?php if ($address): ?>
          <div>
            <h2 class="font-heading text-2xl text-primary mb-3"><?= t('contact.address') ?></h2>
            <p class="text-light/70"><?= nl2br(Str::esc($address)) ?></p>
            <?php if ($page->directions_url()->isNotEmpty()): ?>
              <a href="<?= $page->directions_url() ?>" target="_blank" rel="noopener"
                 class="btn-outline inline-block mt-4 text-xs">
                <?= t('contact.directions') ?>
              </a>
            <?php endif ?>
          </div>
        <?php endif ?>

        <!-- Opening hours per CONT-02 -->
        <div>
          <h2 class="font-heading text-2xl text-primary mb-3"><?= t('footer.hours') ?></h2>
          <?php if ($hours && $hours->count()): ?>
            <ul class="space-y-2">
              <?php foreach ($hours as $item): ?>
                <li class="text-light/70 flex justify-between">
                  <span><?= $item->day() ?></span>
                  <span class="font-semibold"><?= $item->time() ?></span>
                </li>
              <?php endforeach ?>
            </ul>
          <?php endif ?>
        </div>

        <!-- Phone and WhatsApp per CONT-03 -->
        <div class="flex flex-col gap-3">
          <?php if ($phone): ?>
            <a href="tel:<?= Str::esc($phone) ?>"
               class="btn-primary text-center">
              <?= t('contact.call') ?> <?= Str::esc($phone) ?>
            </a>
          <?php endif ?>
          <?php if ($whatsapp): ?>
            <a href="https://wa.me/<?= str_replace(['+', ' '], '', $whatsapp) ?>"
               class="inline-flex items-center justify-center gap-2 px-6 py-3 bg-[#25D366] text-white font-semibold rounded-md hover:bg-[#20bd5a] transition-colors text-sm uppercase tracking-wider"
               target="_blank" rel="noopener">
              WhatsApp
            </a>
          <?php endif ?>
        </div>

        <!-- Email -->
        <?php if ($email): ?>
          <div>
            <a href="mailto:<?= Str::esc($email) ?>" class="text-primary hover:text-primary-dark transition-colors">
              <?= Str::esc($email) ?>
            </a>
          </div>
        <?php endif ?>
      </div>
    </div>
  </section>
</main>
<?php snippet('layout/footer') ?>
```

**Create `site/snippets/sections/contact-map.php`** per D-08 (Leaflet + openstreetmap.de, GDPR-safe):

```php
<!-- Leaflet CSS -->
<link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css"
      integrity="sha256-p4NxAoJBhIIN+hmNHrzRCf9tD/miZyoHS5obTRR9BMY=" crossorigin="" />

<div id="map" class="h-80 md:h-full min-h-[320px] rounded-lg z-0"></div>

<!-- Leaflet JS -->
<script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"
        integrity="sha256-20nQCchB9co0qIjJZRGuk2/Z9VM+kNiyxNV1lvTlZBo=" crossorigin=""></script>
<script>
  document.addEventListener('DOMContentLoaded', function() {
    var lat = <?= floatval($lat) ?>;
    var lng = <?= floatval($lng) ?>;
    var map = L.map('map').setView([lat, lng], 16);
    L.tileLayer('https://{s}.tile.openstreetmap.de/{z}/{x}/{y}.png', {
      maxZoom: 19,
      attribution: '&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a>'
    }).addTo(map);
    L.marker([lat, lng]).addTo(map)
      .bindPopup('<strong><?= Str::esc($popup) ?></strong>')
      .openPopup();
  });
</script>
```

**Create `content/5_contact/` directory** and bilingual content files:

**`content/5_contact/contact.it.txt`:**
```
Title: Contatti
----
Headline: Dove Trovarci
----
Intro: Siamo nel cuore del centro storico di Este, a due passi dal Castello Carrarese. Venite a trovarci per un aperitivo o una cena in compagnia.
----
Map_lat: 45.2272
----
Map_lng: 11.6574
----
Map_popup: Porta Vecia - Enoteca e Cucina Cinese
----
Directions_url: https://www.google.com/maps/dir/?api=1&destination=45.2272,11.6574
```

**`content/5_contact/contact.en.txt`:**
```
Title: Contact
----
Headline: Find Us
----
Intro: We are in the heart of Este's historic center, steps from the Carrarese Castle. Come visit us for an aperitivo or dinner.
----
Map_popup: Porta Vecia - Wine Bar & Chinese Cuisine
```

Note: map_lat, map_lng, directions_url are `translate: false` so they only appear in the default language file.
  </action>
  <verify>
    <automated>grep -q "map_lat" site/blueprints/pages/contact.yml && grep -q "openstreetmap.de" site/snippets/sections/contact-map.php && grep -q "contact-map" site/templates/contact.php && grep -q "wa.me" site/templates/contact.php && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - site/blueprints/pages/contact.yml contains fields: headline, intro, map_lat, map_lng, map_popup, directions_url
    - site/blueprints/pages/contact.yml has `translate: false` on map_lat, map_lng, directions_url
    - site/templates/contact.php contains `snippet('sections/contact-map'`
    - site/templates/contact.php renders `$hours` loop for opening hours (CONT-02)
    - site/templates/contact.php contains `tel:` link for phone (CONT-03)
    - site/templates/contact.php contains `wa.me/` link for WhatsApp (CONT-03)
    - site/templates/contact.php contains `$page->directions_url()` (CONT-04)
    - site/templates/contact.php uses `grid-cols-1 md:grid-cols-2` for split layout (D-09)
    - site/snippets/sections/contact-map.php contains `unpkg.com/leaflet@1.9.4`
    - site/snippets/sections/contact-map.php contains `openstreetmap.de` tile URL (not openstreetmap.org)
    - site/snippets/sections/contact-map.php contains `L.map('map')` and `L.marker`
    - content/5_contact/contact.it.txt contains `Map_lat: 45.2272`
    - content/5_contact/contact.en.txt contains `Headline: Find Us`
  </acceptance_criteria>
  <done>About page renders 4 alternating storytelling blocks with fusion concept section. Contact page shows OpenStreetMap (Leaflet, GDPR-safe), opening hours, one-tap WhatsApp/phone buttons, address with Google Maps directions link. Both pages bilingual.</done>
</task>

</tasks>

<verification>
- About page loads with 4 alternating text/image sections
- About page includes "Due Tradizioni, Un Tavolo" fusion section
- Contact page shows interactive OpenStreetMap with marker
- Contact page displays opening hours from global site data
- Contact page has clickable phone and WhatsApp buttons
- Contact page has "Indicazioni" button linking to Google Maps
- Split layout on desktop (map left, info right), stacked on mobile
- All text in both IT and EN
</verification>

<success_criteria>
- About page tells the Porta Vecia story through 4 alternating visual blocks
- Contact page provides all methods to reach the venue (map, phone, WhatsApp, directions)
- OpenStreetMap uses openstreetmap.de tiles (GDPR-safe, no consent needed)
- All content editable from Kirby Panel
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pages/02-04-SUMMARY.md`
</output>
