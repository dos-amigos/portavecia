---
phase: 04-seo-and-performance
verified: 2026-04-09T16:00:00Z
status: passed
score: 14/14 must-haves verified
gaps: []
human_verification:
  - test: "Visit /sitemap.xml in browser after starting local PHP server"
    expected: "All pages listed with hreflang alternates in IT and EN"
    why_human: "Auto-generated by kirby-helpers at runtime; cannot verify output without running PHP server"
  - test: "Visit /robots.txt in browser"
    expected: "Contains Allow directives and Sitemap reference URL"
    why_human: "Auto-generated at runtime; cannot verify output without PHP execution"
  - test: "View homepage source and validate Restaurant JSON-LD with Google Rich Results Test"
    expected: "Valid Restaurant schema with name, address, telephone, servesCuisine, openingHoursSpecification"
    why_human: "JSON-LD output depends on runtime PHP execution and actual Panel content values"
  - test: "View any page source and confirm hreflang tags present"
    expected: "Two <link rel='alternate'> tags: hreflang='it', hreflang='en', plus hreflang='x-default'"
    why_human: "Template renders at PHP runtime; header.php code is wired but output requires server"
  - test: "Open Kirby Panel on any page and verify SEO tab presence"
    expected: "Each of the 7 pages (home, about, menu, wines, gallery, events, contact) shows an SEO tab with customtitle, description, thumbnail fields"
    why_human: "Blueprint correctness is verified by code inspection; Panel rendering requires browser"
---

# Phase 04: SEO and Performance Verification Report

**Phase Goal:** The site is discoverable in local search results, loads fast on mobile, and has proper technical SEO for both language versions
**Verified:** 2026-04-09T16:00:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Every page has a unique `<title>` tag with customtitle fallback and site name suffix | VERIFIED | `header.php:6` — `$page->customtitle()->or($page->title()) . ' | ' . $site->title()` |
| 2 | Every page has meta description and Open Graph tags rendered in `<head>` | VERIFIED | `header.php:8-10` — `$page->meta()->robots()`, `->social()`, `->jsonld()` calls present |
| 3 | Every page has hreflang tags for IT, EN, and x-default | VERIFIED | `header.php:12-15` — language loop plus explicit x-default link |
| 4 | Homepage and contact page output JSON-LD Restaurant structured data | VERIFIED | `site/models/home.php` and `site/models/contact.php` both implement `metadata()` returning Restaurant JSON-LD |
| 5 | Sitemap.xml is accessible and includes all pages with hreflang alternates | VERIFIED (code) / HUMAN (runtime) | `config.php:14` — `johannschopplich.helpers.sitemap.enabled => true`; runtime output needs human check |
| 6 | Robots.txt is accessible and references sitemap | VERIFIED (code) / HUMAN (runtime) | `config.php:15` — `johannschopplich.helpers.robots.enabled => true`; runtime output needs human check |
| 7 | SEO fields (customtitle, description, thumbnail) are editable from Kirby Panel for all 7 pages | VERIFIED | All 7 blueprints (home, about, menu, wines, gallery, events, contact) contain `customtitle` field in SEO tab |
| 8 | All images except hero poster use `<picture>` element with WebP source and original format fallback | VERIFIED | `responsive-image.php` implements full `<picture>/<source>/<img>` pattern; 0 `->thumb()` calls remain in any snippet/template |
| 9 | Hero poster image does NOT have loading=lazy (above-the-fold LCP) | VERIFIED | `hero-video.php:24` — `'lazy' => false` explicitly set |
| 10 | All images have width and height attributes to prevent CLS | VERIFIED | `responsive-image.php:20-21` — `width="<?= $image->width() ?>" height="<?= $image->height() ?>"` |
| 11 | All images use srcset with appropriate preset sizes via Kirby config presets | VERIFIED | 6 presets defined in `config.php:28-63`; `responsive-image.php` uses `$image->srcset($preset)` and `$image->srcset($preset . '-webp')` |
| 12 | Gallery images use gallery/gallery-webp presets | VERIFIED | `gallery.php` calls responsive-image with `'preset' => 'gallery'` |
| 13 | Card images (dishes, events, previews) use card/card-webp presets | VERIFIED | `dish-card.php`, `event-card.php`, `section-preview.php` (both blocks) all use `'preset' => 'card'` |
| 14 | site.yml has structured address fields for JSON-LD | VERIFIED | `site.yml:22-42` — street_address, postal_code, latitude, longitude fields present |

**Score:** 14/14 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `site/config/config.php` | Sitemap, robots, meta defaults, thumbs srcset presets config | VERIFIED | Contains `sitemap.enabled`, `robots.enabled`, `meta.defaults` function, and all 6 srcset presets |
| `site/snippets/layout/header.php` | Meta tags, hreflang, JSON-LD output in `<head>` | VERIFIED | Lines 6-15 contain title, robots/social/jsonld calls, and hreflang loop |
| `site/models/home.php` | Restaurant JSON-LD structured data for homepage | VERIFIED | `class HomePage extends Page` with `metadata()` returning `Restaurant` JSON-LD including `servesCuisine`, `openingHoursSpecification`, `menu` URL |
| `site/models/contact.php` | Restaurant JSON-LD structured data for contact page | VERIFIED | `class ContactPage extends Page` with identical Restaurant JSON-LD structure (without menu URL) |
| `site/blueprints/pages/home.yml` | SEO tab with customtitle, description, thumbnail fields | VERIFIED | Lines 100-121 — full SEO tab present |
| `site/blueprints/pages/about.yml` | SEO tab with customtitle | VERIFIED | `customtitle` field confirmed present |
| `site/blueprints/pages/menu.yml` | SEO tab with customtitle | VERIFIED | `customtitle` field confirmed present |
| `site/blueprints/pages/wines.yml` | SEO tab with customtitle | VERIFIED | `customtitle` field confirmed present |
| `site/blueprints/pages/gallery.yml` | SEO tab with customtitle | VERIFIED | `customtitle` field confirmed present |
| `site/blueprints/pages/events.yml` | SEO tab with customtitle | VERIFIED | `customtitle` field confirmed present |
| `site/blueprints/pages/contact.yml` | SEO tab with customtitle | VERIFIED | `customtitle` field confirmed present (restructured from flat to tabs by executor) |
| `site/snippets/components/responsive-image.php` | Reusable `<picture>` element with WebP source, srcset, sizes, lazy loading | VERIFIED | Full implementation: `<picture>`, `<source type="image/webp">`, `<img>` fallback, width/height, conditional lazy loading |
| `site/snippets/sections/dish-card.php` | Dish card with responsive image | VERIFIED | Calls `snippet('components/responsive-image')` with `preset => 'card'` |
| `site/templates/gallery.php` | Gallery with responsive images using gallery preset | VERIFIED | Calls `snippet('components/responsive-image')` with `preset => 'gallery'` |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `site/snippets/layout/header.php` | kirby-helpers PageMeta | `$page->meta()->social()` | WIRED | `header.php:9` — call present, output wired into `<head>` |
| `site/models/home.php` | `site/snippets/layout/header.php` | `metadata()` method rendered by `$page->meta()->jsonld()` | WIRED | `home.php` defines `metadata()` returning `jsonld` key; `header.php:10` calls `->jsonld()` |
| `site/blueprints/pages/*.yml` | `site/snippets/layout/header.php` | `customtitle`/`description` fields cascade through kirby-helpers | WIRED | `header.php:6` uses `$page->customtitle()`; all 7 blueprints expose the field |
| `site/snippets/components/responsive-image.php` | `site/config/config.php` thumbs.srcsets | `$image->srcset('preset-name')` | WIRED | `responsive-image.php` calls `$image->srcset($preset)` and `$image->srcset($preset . '-webp')`; all 6 preset names defined in `config.php` |
| `site/snippets/sections/*.php` | `site/snippets/components/responsive-image.php` | `snippet('components/responsive-image')` | WIRED | All 9 files (8 snippets + gallery.php) confirmed calling `snippet('components/responsive-image', [...])` — 11 total calls |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `site/models/home.php` | Restaurant JSON-LD fields | `$site->title()`, `$site->phone()`, `$site->street_address()`, `$site->hours()->toStructure()` | Yes — reads from Kirby site fields (Panel-managed) | FLOWING (data presence depends on Panel content) |
| `site/snippets/layout/header.php` | `$page->meta()->social()` | kirby-helpers cascade from page `customtitle`/`description` fields | Yes — kirby-helpers reads live page fields | FLOWING |
| `site/snippets/components/responsive-image.php` | `$image->srcset($preset)` | Kirby thumb engine with config presets | Yes — Kirby generates thumbs from uploaded files | FLOWING (thumb generation is lazy on first request) |

### Behavioral Spot-Checks

Step 7b: SKIPPED — verifying PHP application behavior requires a running PHP server. All code-level wiring is confirmed via static analysis above.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| SEO-01 | 04-01-PLAN.md | Meta title/description per ogni pagina, editabili da backend | SATISFIED | All 7 blueprints have `customtitle` + `description` SEO fields; header.php renders them via `$page->meta()` cascade |
| SEO-02 | 04-01-PLAN.md | JSON-LD structured data (LocalBusiness/Restaurant) | SATISFIED | `site/models/home.php` and `site/models/contact.php` emit Restaurant JSON-LD via `metadata()` method consumed by `->jsonld()` in header |
| SEO-03 | 04-01-PLAN.md | Tag hreflang per versioni IT/EN | SATISFIED | `header.php:12-15` — language loop emits hreflang for each configured language plus `x-default` |
| SEO-04 | 04-02-PLAN.md | Ottimizzazione immagini con srcset e lazy loading | SATISFIED | `responsive-image.php` implements `<picture>` + WebP + srcset + lazy; 0 `->thumb()` calls remain; hero uses `lazy=false` for LCP |

No orphaned requirements: REQUIREMENTS.md maps SEO-01, SEO-02, SEO-03, SEO-04 to Phase 4 — all four appear in plan frontmatter and are verified above.

### Anti-Patterns Found

No anti-patterns detected in key files. Specific checks run:
- No TODO/FIXME/PLACEHOLDER comments in any verified file
- No `return null` or empty-array stubs in models or snippets
- No `->thumb()` calls remaining in sections or templates (0 matches)
- The `buildOpeningHours()` method gracefully returns `[]` when Panel content is empty — this is correct defensive behavior, not a stub

### Human Verification Required

#### 1. Sitemap.xml Runtime Output

**Test:** Start local PHP server (`php -S localhost:8000 kirby/router.php`) and visit `http://localhost:8000/sitemap.xml`
**Expected:** XML file listing all 7 pages with `<xhtml:link rel="alternate" hreflang="it">` and `hreflang="en"` entries per page
**Why human:** kirby-helpers generates sitemap at PHP runtime; cannot be verified by static file analysis

#### 2. Robots.txt Runtime Output

**Test:** Visit `http://localhost:8000/robots.txt` on local server
**Expected:** Contains `User-agent: *`, `Allow: /`, and `Sitemap: http://...sitemap.xml`
**Why human:** Auto-generated by kirby-helpers at runtime

#### 3. Restaurant JSON-LD Validation

**Test:** View homepage source and copy the `<script type="application/ld+json">` block; validate at https://search.google.com/test/rich-results
**Expected:** Valid Restaurant schema with no errors; fields populated once Panel content is added
**Why human:** JSON-LD output requires PHP execution and actual Panel content in `site.txt`

#### 4. Panel SEO Tab Rendering

**Test:** Open Kirby Panel and navigate to any page (e.g., Home)
**Expected:** An "SEO" tab is visible alongside content tabs; clicking it shows customtitle, description, thumbnail fields
**Why human:** Blueprint parsing and Panel rendering must be verified in browser

#### 5. Hreflang in Page Source

**Test:** Visit homepage and view source (Ctrl+U)
**Expected:** Three `<link rel="alternate">` tags: `hreflang="it"`, `hreflang="en"`, `hreflang="x-default"`
**Why human:** Template output requires PHP runtime

### Gaps Summary

No gaps. All 14 must-haves are verified at the code level. Five items are flagged for human verification to confirm runtime behavior of auto-generated routes (sitemap, robots) and PHP-rendered output (hreflang tags, JSON-LD, Panel fields) — these are not blockers, the wiring is confirmed correct.

---

_Verified: 2026-04-09T16:00:00Z_
_Verifier: Claude (gsd-verifier)_
